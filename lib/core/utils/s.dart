// ============================================================================
// MOVED INTO THE SERVICES MODULE.
// This was services-management code that lived in core but referenced module
// types, so core broke whenever the services module was deleted. The real
// implementation now lives under features/services_management_module/ and is
// removed together with the module. Kept empty so core no longer depends on it.
// ============================================================================
