import 'package:get/get.dart';

import 'package:flutter/material.dart';
import 'package:grc_module/generated/l10n.dart';

// Stub: HomePageHelper — module not included in knowticed.
class HomePageHelper extends StatelessWidget {
  const HomePageHelper({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text(S.current.homepagehelper)));
}
