import 'package:get/get.dart';

// Stub: TodoModel
// The original todo module was removed; this minimal model keeps the home
// schedule controller compiling. Extend with real fields if the module returns.
class TodoModel {
  TodoModel();
}

// Stub: TodoController
// Provides the members referenced by ScheduleController.
class TodoController extends GetxController {
  List<TodoModel> allTodoList = [];
}
